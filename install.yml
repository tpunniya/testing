---
- name: Install community.general collection
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Install community.general collection
      community.general.ansible_galaxy:
        name: community.general
        state: present
